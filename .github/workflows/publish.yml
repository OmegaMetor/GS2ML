name: Publish build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build
    uses: ./.github/workflows/cmake-single-platform.yml

  publish:
    name: Publish
    runs-on: ubuntu-latest
    needs: build
    steps:
    - uses: actions/download-artifact@v3
      with:
        path: ${{ github.workspace }}/artifacts

    - uses: vimtor/action-zip@v1
      with:
        files: artifacts/gs2ml-win64
        dest: gm2ml-win64.zip

    - name: Set Short SHA
      id: set_short_sha
      run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.set_short_sha.outputs.short_sha }}
        release_name: Development release, commit ${{ steps.set_short_sha.outputs.short_sha }}
        draft: false
        prerelease: false

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.github_token }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./gm2ml-win64.zip
 
